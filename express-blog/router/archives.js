const express = require("express"),
    model = require("../module/model"),
    {Article , Tag} = model,
    router = express.Router();

router.get("/" , (req , res) => {
    (async () => {
        
    })();
})

module.exports = router;