const express = require("express"),
    {User, Comment} = require("../module/model"),
    router = express.Router();

router.get("/:article_id", (req, res) => {
    (async () => {
        
    })();
});

module.exports = router;