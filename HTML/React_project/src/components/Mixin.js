import React from 'react';

class Binging extends React.Component {
    constructor (props) {
        super(props);
        this.state = {

        };
    }
    render () {
        return (
            <div>
                <input type="text" onChange={this.} />
                <p>{this.state.text}</p>
            </div>
        );
    }
}