<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
    <!--<form action="/reg" method="GET">
        <input type="text" name="name" placeholder="请输入您的账户名"/>
        <input type="password" name="pwd" placeholder="请输入您的密码" />
        <input type="submit" />
    </form>-->
    <!--<% for(let obj of data){ %>
        <% for(let [key,value] of Object.entries(obj)){ %>
            <%= `${key} : ${value}` %>
        <% } %>
        <br />
    <% } %>-->
    <input class="name" type="text" name="name" placeholder="请输入您的账户名"/>
    <input class="pwd" type="password" name="pwd" placeholder="请输入您的密码" />
    <input class="submit" type="submit" />
    <script src="js/jquery-2.2.0.min.js"></script>
    <script>
        $('.submit').click(function(){
            $.ajax({
                url: '/reg',
                type: 'get',
                data: {
                    name: $('.name').val(),
                    pwd: $('.pwd').val()
                },
                success: function(data){
                    console.log(data);
                }
            });
        });
    </script>
</body>
</html>